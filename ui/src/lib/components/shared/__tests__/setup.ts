/**
 * Test setup for shared component tests.
 *
 * Extends vitest matchers with @testing-library/jest-dom for DOM assertions
 * (e.g., toBeInTheDocument, toHaveTextContent).
 */
import "@testing-library/jest-dom/vitest";
